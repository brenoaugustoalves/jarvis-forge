{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'if [ \"${WORKER_MODE:-0}\" = \"1\" ]; then exec python worker.py; else exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}; fi'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ALWAYS"
  }
}
